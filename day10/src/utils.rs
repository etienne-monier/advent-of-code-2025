// fn reduce_vec_of_sets<T>(input: Vec<HashSet<T>>) {
//     for n in 1..input.len() {
//         // We are using n buttons. We need to have all combinations of n buttons
//         let mut combos = Combinations::of_size(0..input.len(), n);

//         for combo in combos {
//             let combo_c = combo.cl
//             combo.sort_by(|&a, &b| input[a].len().cmp(&input[b].len()));
//         }
//     }
// }
